{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "buildCommand": "NEXT_PUBLIC_APP_URL=https://$VERCEL_URL bunx turbo build --filter=sim...",
    "installCommand": "bun install",
    "framework": null,
    "outputDirectory": "apps/sim/.next",
    "functions": {
        "apps/sim/app/api/**/*.ts": {
            "maxDuration": 300
        }
    }
}