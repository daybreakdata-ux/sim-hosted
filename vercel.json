{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "buildCommand": "bunx turbo build --filter=@sim/sim...",
    "installCommand": "bun install",
    "framework": null,
    "outputDirectory": "apps/sim/.next",
    "functions": {
        "apps/sim/app/api/**/*.ts": {
            "maxDuration": 300
        }
    }
}